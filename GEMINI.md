# Agent instructions

Read `README.md`, `spec/MINEX_1000_CORE_SPEC_DRAFT_CN_EN.md`, and `docs/SECURITY_AND_AUTHORITY_CN_EN.md` before changing the reference core.

Hard constraints:

- preserve `automatic_external_action_authority = 0`;
- do not add unrestricted shell execution;
- do not accept caller-controlled local filesystem paths through the HTTP API;
- do not treat GUI accessibility as permission;
- do not collapse physical energy, money, time, human attention, privacy, carbon, semantic loss, and failure into one unexplained number;
- update tests, schemas, limitations, and receipts when behavior changes;
- retain adverse findings rather than deleting them from validation.
