# Formal model boundary

`MINEX.tla` captures the reference lifecycle: draft, plan, optional lease, local or proposed execution, verification, settlement proposal, and stop. It asserts that external authority and automatic payment remain zero. The release includes a separate bounded Python explorer; the TLA+ model is not claimed to have been checked by TLC in this build environment.
